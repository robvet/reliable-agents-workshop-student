# Vocab to Add

## teaching payload

- is the deterministic-control patterns (model proposes, code validates; typed pipeline; ReAct with corroboration; the whole night's work on asset/outage status). The data model is scaffolding for that, not the point.

- The spine becomes assets + outages only, that's a real retirement list: customer_agent, reliability_agent, and the CUSTOMER_STATUS/RELIABILITY/MAJOR_EVENT intents (and probably event_agent/crew_agent fold into outage rather than staying separate, since outage restoration is event+crew in this domain).
