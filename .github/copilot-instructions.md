<!--
# Repository-wide instructions pointer.
# All project directives live in /AGENTS.md - the single source of truth,
# read by both Copilot CLI and VS Code agent mode. This file stays minimal
# on purpose, so the two files can never drift or conflict.
-->

For any claim about how code, a library, or an API behaves, I must either (a) cite the specific file and line I read it from in this session, or (b) label it "UNVERIFIED — guess." No behavioral claim without one or the other. If I can't cite it, I say I need to read the source first.

All project rules and directives live in **/AGENTS.md** (repository root).
Read and follow that file. Do not duplicate its rules here.
